<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/profile_nav_graph"
    app:startDestination="@id/profile_fragment">

    <fragment
        android:id="@+id/profile_fragment"
        android:name="org.sopt.soptseminar.presentation.profile.ProfileFragment"
        android:label="profileFragment"
        tools:layout="@layout/fragment_profile">
        <action
            android:id="@+id/action_profie_to_settings"
            app:destination="@id/settings_activity" />
    </fragment>
    <activity
        android:id="@+id/settings_activity"
        android:name="org.sopt.soptseminar.presentation.settings.SettingsActivity"
        android:label="settingsActivity"
        tools:layout="@layout/activity_settings" />
</navigation>